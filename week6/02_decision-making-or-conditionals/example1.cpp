#include <iostream>

int main() {
    int num = 10;

    // Using if statement to check if a number is positive
    if (num > 0) {
        std::cout << "Number is positive." << std::endl;
    }

    return 0;
}
