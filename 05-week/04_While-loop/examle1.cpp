#include <iostream>

int main() {
    int num = 1;

    // Using while loop to print numbers from 1 to 5
    while (num <= 5) {
        std::cout << num << " ";
        ++num;  // Incrementing num
    }

    std::cout << std::endl;

    return 0;
}
